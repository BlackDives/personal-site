import React from "react";
import ResponsiveBlock from "~components/shared/responsive-block";
var Home = function () {
    return React.createElement(ResponsiveBlock, null, "'Ello!");
};
export default Home;
